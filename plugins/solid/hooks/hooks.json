{
  "description": "APEX SOLID hooks - project detection only. SOLID file-size + interface enforcement is delegated to @fusengine/harness via core-guards (duplicate validate-solid/check-file-size removed to avoid a double-gate).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [{
          "type": "command",
          "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/detect-project.py",
          "timeout": 5
        }]
      }
    ]
  }
}
